{
  "name": "playground",
  "version": "1.0.0",
  "description": "이것저것 소규모로 구현하는 곳",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "ts-node-dev --respawn --transpile-only app.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
